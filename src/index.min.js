/* early-errors v0.1.0 */
(function (r, a) {
  if (r.__earlyerrors__) return;
  (r.__earlyerrors__ = !0), (a = Object.assign({ max: 50 }, a));
  var c = i('error'),
    d = i('unhandledrejection'),
    v = r.addEventListener;
  r.addEventListener = function (t, e, u) {
    return t === 'error' && c(e), t === 'unhandledrejection' && d(e), v.call(r, t, e, u);
  };
  function i(t) {
    var e = [],
      u = !1,
      s;
    r.addEventListener(t, function (n) {
      !u && e.length < a.max && e.push(n);
    });
    var f = 'on' + t;
    (r[f] = function () {
      if (s) return s.apply(r, arguments);
    }),
      Object.defineProperty(r, f, {
        get: function () {
          return s;
        },
        set: function (n) {
          (s = n), l(n);
        },
      });
    function l(n) {
      for (u = !0; e.length; )
        try {
          var o = e.shift();
          t === 'error' && n === s ? n(o.message, o.filename, o.lineno, o.colno, o.error) : n(o);
        } catch (h) {
          console.error(h);
        }
    }
    return l;
  }
})(window);
